{
  "name": "pinterest-connector-cloud",
  "version": "1.0.0",
  "description": "Pinterest API board sync connector for Codex design reference workflows.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "smoke": "node -e \"require('./src/server'); setTimeout(() => process.exit(0), 500)\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "archiver": "^7.0.1",
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  }
}
